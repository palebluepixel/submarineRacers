#ifndef FORM_HXX
#define FORM_HXX

#include <glm/glm.hpp>


/**
 *
 * A form is a structure encapsulating all information
 * an object has regarding its position in the physical
 * world. This includes, position, velocity, bounding 
 * geometry (or null if intangible), orientation,
 * angular velocity, ...
 *
 */
class Form{
  
};

#endif